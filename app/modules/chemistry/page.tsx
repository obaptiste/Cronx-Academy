import ChemistryInteractive from '@/components/modules/ChemistryInteractive';

export const metadata = {
  title: 'Chemistry Interactive — Cronx Academy',
  description:
    'Interactive chemistry lessons covering atomic structure, the periodic table, bonding, reactions, acids & bases, and separation techniques.',
};

export default function ChemistryPage() {
  return <ChemistryInteractive />;
}
